# —— Python / 后端 ——
__pycache__/
*.py[cod]
.venv/
venv/
*.db
backend/aragon.db
# WAL 附属文件与 purge 工具的备份不入库（`*.db` 不覆盖这三种后缀）。
*.db-wal
*.db-shm
*.db.bak-*
# 文档 blob 根目录（ticket-document-management §5.3）：用户上传的文件不入库。
backend/var/

# —— Node / 前端 ——
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# —— 环境变量 ——
.env
.env.*
!.env.local.example

# —— 编辑器 / 系统 ——
.DS_Store
Thumbs.db
.idea/
.vscode/

# —— Agent 运行时 / 工具目录（非项目源码，勿提交）——
.agentmesh/
.claude/
